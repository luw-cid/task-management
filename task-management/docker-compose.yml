services:
  task-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-backend
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0
